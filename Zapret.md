## 🛠️ Альтернативный подбор стратегий с помощью GoodCheck (Блокчек)

Если стандартные тесты в `service.bat` не находят рабочую стратегию обхода для вашего провайдера, рекомендуется использовать стороннюю утилиту **GoodCheck**. Это продвинутый автоматический сканер параметров, совместимый с Zapret, GoodbyeDPI и ByeDPI.

### 📥 Подготовка и настройка
1. Скачайте актуальную версию **GoodCheck** с официальной ветки форума [NTC Party](https://ntc.party/t/goodcheck-%D0%B1%D0%BB%D0%BE%D0%BA%D1%87%D0%B5%D0%BA-%D0%B4%D0%BB%D1%8F-gdpi-zapret-byedpi-%D0%B0%D0%BA%D1%82%D1%83%D0%B0%D0%BB%D1%8C%D0%BD%D0%B0%D1%8F-%D0%B2%D0%B5%D1%80%D1%81%D0%B8%D1%8F-%D0%B2-%D0%BF%D1%80%D0%BE%D1%84%D0%B8%D0%BB%D0%B5/10880).
2. Распакуйте архив GoodCheck в **отдельную папку** (не смешивайте с файлами данного репозитория).
3. Откройте файл `config.cmd` в папке GoodCheck через Блокнот.
4. Найдите строку переопределения пути для Zapret и укажите полный путь к папке с вашей утилитой `winws.exe` (находится внутри папки `zapret` данного проекта):
   ```cmd
   set "_zapretFolderOverride=C:\Ваш_Путь_К_Папке\zapret-discord-youtube\zapret\bin\x86_64"
   ```
   *Убедитесь, что путь взят в кавычки, если внутри есть пробелы.*

### 🚀 Запуск тестирования
1. Перед началом тестов **обязательно остановите и удалите текущую службу** через наш скрипт: `service.bat` -> `Remove Services`. Две одновременно работающие утилиты заблокируют сетевой драйвер `WinDivert`.
2. Запустите `GoodCheck.cmd` (или `start.cmd`, в зависимости от сборки).
3. Выберите режим проверки **Zapret (винвс)** и укажите необходимый чеклист сайтов (например, `youtube` или универсальный).
4. Дождитесь окончания сканирования. Утилита поочередно протестирует сотни комбинаций флагов обфускации трафика.

### 💾 Применение результатов
После завершения проверки программа сформирует лог-файл в папке `Logs/`. 
1. Найдите строки со статусом **[SUCCESS]**.
2. Скопируйте рабочую комбинацию аргументов (например: `--dpi-desync=fake,multidisorder --dpi-desync-fooling=md5sig`).
3. Откройте `service.bat` из нашего репозитория, перейдите в меню кастомной настройки и вставьте полученные аргументы для создания кастомной службы.
